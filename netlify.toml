[build]
  command = "npm ci && npm run build"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.157.0"
  HUGO_EXTENDED = "true"
  HUGO_SKIP_DOWNLOAD = "true"
  HUGO_BIN_PATH = "/opt/buildhome/bin/hugo"
